require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe ReportingForms::TanimotosController do

  # This should return the minimal set of attributes required to create a valid
  # ReportingForms::Tanimoto. As you add validations to ReportingForms::Tanimoto, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    {}
  end
  
  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # ReportingForms::TanimotosController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET index" do
    it "assigns all reporting_forms_tanimotos as @reporting_forms_tanimotos" do
      tanimoto = ReportingForms::Tanimoto.create! valid_attributes
      get :index, {}, valid_session
      assigns(:reporting_forms_tanimotos).should eq([tanimoto])
    end
  end

  describe "GET show" do
    it "assigns the requested tanimoto as @tanimoto" do
      tanimoto = ReportingForms::Tanimoto.create! valid_attributes
      get :show, {:id => tanimoto.to_param}, valid_session
      assigns(:tanimoto).should eq(tanimoto)
    end
  end

  describe "GET new" do
    it "assigns a new tanimoto as @tanimoto" do
      get :new, {}, valid_session
      assigns(:tanimoto).should be_a_new(ReportingForms::Tanimoto)
    end
  end

  describe "GET edit" do
    it "assigns the requested tanimoto as @tanimoto" do
      tanimoto = ReportingForms::Tanimoto.create! valid_attributes
      get :edit, {:id => tanimoto.to_param}, valid_session
      assigns(:tanimoto).should eq(tanimoto)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new ReportingForms::Tanimoto" do
        expect {
          post :create, {:tanimoto => valid_attributes}, valid_session
        }.to change(ReportingForms::Tanimoto, :count).by(1)
      end

      it "assigns a newly created tanimoto as @tanimoto" do
        post :create, {:tanimoto => valid_attributes}, valid_session
        assigns(:tanimoto).should be_a(ReportingForms::Tanimoto)
        assigns(:tanimoto).should be_persisted
      end

      it "redirects to the created tanimoto" do
        post :create, {:tanimoto => valid_attributes}, valid_session
        response.should redirect_to(ReportingForms::Tanimoto.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved tanimoto as @tanimoto" do
        # Trigger the behavior that occurs when invalid params are submitted
        ReportingForms::Tanimoto.any_instance.stub(:save).and_return(false)
        post :create, {:tanimoto => {}}, valid_session
        assigns(:tanimoto).should be_a_new(ReportingForms::Tanimoto)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        ReportingForms::Tanimoto.any_instance.stub(:save).and_return(false)
        post :create, {:tanimoto => {}}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested tanimoto" do
        tanimoto = ReportingForms::Tanimoto.create! valid_attributes
        # Assuming there are no other reporting_forms_tanimotos in the database, this
        # specifies that the ReportingForms::Tanimoto created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        ReportingForms::Tanimoto.any_instance.should_receive(:update_attributes).with({'these' => 'params'})
        put :update, {:id => tanimoto.to_param, :tanimoto => {'these' => 'params'}}, valid_session
      end

      it "assigns the requested tanimoto as @tanimoto" do
        tanimoto = ReportingForms::Tanimoto.create! valid_attributes
        put :update, {:id => tanimoto.to_param, :tanimoto => valid_attributes}, valid_session
        assigns(:tanimoto).should eq(tanimoto)
      end

      it "redirects to the tanimoto" do
        tanimoto = ReportingForms::Tanimoto.create! valid_attributes
        put :update, {:id => tanimoto.to_param, :tanimoto => valid_attributes}, valid_session
        response.should redirect_to(tanimoto)
      end
    end

    describe "with invalid params" do
      it "assigns the tanimoto as @tanimoto" do
        tanimoto = ReportingForms::Tanimoto.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        ReportingForms::Tanimoto.any_instance.stub(:save).and_return(false)
        put :update, {:id => tanimoto.to_param, :tanimoto => {}}, valid_session
        assigns(:tanimoto).should eq(tanimoto)
      end

      it "re-renders the 'edit' template" do
        tanimoto = ReportingForms::Tanimoto.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        ReportingForms::Tanimoto.any_instance.stub(:save).and_return(false)
        put :update, {:id => tanimoto.to_param, :tanimoto => {}}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested tanimoto" do
      tanimoto = ReportingForms::Tanimoto.create! valid_attributes
      expect {
        delete :destroy, {:id => tanimoto.to_param}, valid_session
      }.to change(ReportingForms::Tanimoto, :count).by(-1)
    end

    it "redirects to the reporting_forms_tanimotos list" do
      tanimoto = ReportingForms::Tanimoto.create! valid_attributes
      delete :destroy, {:id => tanimoto.to_param}, valid_session
      response.should redirect_to(reporting_forms_tanimotos_url)
    end
  end

end
