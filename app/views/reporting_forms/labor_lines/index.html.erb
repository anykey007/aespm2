<h1>Строки </h1>

<table>
  <tr>
    <th>Название</th>
    <th>Код</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lines.each do |reporting_forms_labor_line| %>
  <tr>
    <td><%= reporting_forms_labor_line.name_for_select_tag %></td>
    <td><%= reporting_forms_labor_line.code %></td>
    <td><%= link_to 'Show', reporting_forms_labor_line %></td>
    <td><%= link_to 'Edit', edit_reporting_forms_labor_line_path(reporting_forms_labor_line) %></td>
    <td><%= link_to 'Destroy', reporting_forms_labor_line, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Labor line', new_reporting_forms_labor_line_path %>
