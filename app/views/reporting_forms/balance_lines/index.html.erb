<h1>Строки Баланса</h1>
<%= link_to 'New Line', new_reporting_forms_balance_line_path %>
<table>
  <tr>
    <th>Название</th>
    <th>Код</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lines.each do |line| %>
  <tr>
    <td><%= line.name_for_select_tag %></td>
    <td><%= line.code %></td>
    <td><%= link_to 'Show', line %></td>
    <td><%= link_to 'Edit', edit_reporting_forms_balance_line_path(line) %></td>
    <td><%= link_to 'Destroy', line, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Line', new_reporting_forms_balance_line_path %>
