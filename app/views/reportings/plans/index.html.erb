<h1>Фінансові плани</h1>

<table>
  <tr>
    <th>Період</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reportings.each do |reportings_plan| %>
  <tr>
    <td><%= reportings_plan.period %></td>
    <td><%= link_to 'Перегляд', company_reportings_plan_path(params[:company_id],reportings_plan) %></td>
    <td><%= link_to 'Редагування', edit_company_reportings_plan_path(params[:company_id],reportings_plan) %></td>
    <td><%= link_to 'Видалення', company_reportings_plan_path(params[:company_id],reportings_plan), confirm: 'Вы впевнені?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новий', new_company_reportings_plan_path %> |
<%= link_to 'Назад', @company %>
