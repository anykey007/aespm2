<h1>Баланси</h1>

<table>
  <tr>
    <th>Період</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reportings.each do |reportings_balance| %>
  <tr>
    <td><%= reportings_balance.period %></td>
    <td><%= link_to 'Перегляд', company_reportings_balance_path(params[:company_id],reportings_balance) %></td>
    <td><%= link_to 'Редагування', edit_company_reportings_balance_path(params[:company_id],reportings_balance) %></td>
    <td><%= link_to 'Видалення', company_reportings_balance_path(params[:company_id],reportings_balance), confirm: 'Вы впевнені?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новий', new_company_reportings_balance_path %> |
<%= link_to 'Назад', @company %>
