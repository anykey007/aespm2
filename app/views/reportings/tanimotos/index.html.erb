<h1>Танімото</h1>

<table>
  <tr>
    <th>Період</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reportings_tanimotos.each do |reportings_tanimoto| %>
  <tr>
    <td><%= reportings_tanimoto.period %></td>
    <td><%= link_to 'Порівняти', compare_company_reportings_tanimoto_path(params[:company_id],reportings_tanimoto) %></td>
    <td><%= link_to 'Перегляд', company_reportings_tanimoto_path(params[:company_id],reportings_tanimoto) %></td>
    <td><%= link_to 'Редагування', edit_company_reportings_tanimoto_path(params[:company_id],reportings_tanimoto) %></td>
    <td><%= link_to 'Видалення', company_reportings_tanimoto_path(params[:company_id],reportings_tanimoto), confirm: 'Вы впевнені?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новий', new_company_reportings_tanimoto_path %> |
<%= link_to 'Назад', @company %>
