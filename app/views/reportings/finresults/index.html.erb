<h1>Фінансові результати</h1>

<table>
  <tr>
    <th>Період</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reportings.each do |reportings_labor| %>
  <tr>
    <td><%= reportings_labor.period %></td>
    <td><%= link_to 'Перегляд', company_reportings_finresult_path(params[:company_id],reportings_labor) %></td>
    <td><%= link_to 'Редагування', edit_company_reportings_finresult_path(params[:company_id],reportings_labor) %></td>
    <td><%= link_to 'Видалення', company_reportings_finresult_path(params[:company_id],reportings_labor), confirm: 'Вы впевнені?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Новий', new_company_reportings_finresult_path %> |
<%= link_to 'Назад', @company %>
