<h1>Компании</h1>

<table>
  <tr>
    <th>Название</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @companies.each do |company| %>
  <tr>
    <td><%= company.name %></td>
    <td><%= link_to 'Просмотреть', company %></td>
    <td><%= link_to 'Редактировать', edit_company_path(company) %></td>
    <td><%= link_to 'Удалить', company, confirm: 'Вы уверенны?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить компанию', new_company_path %>
