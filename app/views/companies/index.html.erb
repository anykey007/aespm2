<h1>Предприятия</h1>

<table>
  <tr>
    <th>ЕДРПОУ</th>
    <th>Название</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @companies.each do |company| %>
  <tr>
     <td><%= company.EDRPOY %></td>
    <td><%= company.name %></td>
    <td><%= link_to 'Просмотреть', company %></td>
    <td><%= link_to 'Редактировать', edit_company_path(company) %></td>
    <td><%= link_to 'Удалить', company, confirm: 'Вы уверенны?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Добавить предприятие', new_company_path %>
